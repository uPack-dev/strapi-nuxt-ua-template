{
  "collectionName": "components_ui_knopka",
  "info": {
    "displayName": "Кнопка"
  },
  "options": {},
  "attributes": {
    "text": {
      "type": "string"
    },
    "theme": {
      "type": "enumeration",
      "required": true,
      "default": "primary",
      "enum": [
        "primary",
        "secondary"
      ]
    },
    "size": {
      "type": "enumeration",
      "required": true,
      "default": "sm",
      "enum": [
        "sm"
      ]
    },
    "action": {
      "type": "enumeration",
      "required": true,
      "default": "link",
      "enum": [
        "link",
        "link:external",
        "modal:callback"
      ]
    },
    "link": {
      "type": "string"
    }
  },
  "config": {}
}
