/**
 * Карта иконок UiContact: значение `icon` контакта из Strapi → имя иконки CIcon.
 * @type {Record<string, string>}
 */
export const CONTACT_ICONS: Record<string, string> = {
  phone: 'footer/phone',
  email: 'footer/email',
  address: 'menu/location',
};
